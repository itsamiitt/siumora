---
description: "Codify the most recent successful /scrape flow into a permanent browser-skill on disk."
---
Load and run the gstack **skillify** skill for this project.

Read `.claude/skills/gstack/skillify/SKILL.md` in full and follow its instructions, applying them to the current repository. Run any preamble steps it defines first.

Arguments passed to the command: $ARGUMENTS
