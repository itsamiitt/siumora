---
description: "Ship workflow: detect + merge base branch, run tests, review diff, bump VERSION, update CHANGELOG, commit, push, create PR."
---
Load and run the gstack **ship** skill for this project.

Read `.claude/skills/gstack/ship/SKILL.md` in full and follow its instructions, applying them to the current repository. Run any preamble steps it defines first.

Arguments passed to the command: $ARGUMENTS
